sudo: required
language: node_js
node_js:
  - "8"
services:
  - docker
before_install:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
install:
  - yarn install
script:
  - yarn build
  - docker build -t kikoken/frontend-climate .

deploy:
  provider: script
  script:
    docker push kikoken/frontend-climate;
  branch: master